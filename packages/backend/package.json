{
  "name": "@repo/backend",
  "version": "1.0.0",
  "license": "MIT",
  "main": "index.js",
  "exports": {
    "./client": "./client.ts"
  },
  "scripts": {
    "build": "tsc -p convex/tsconfig.json --noEmit",
    "dashboard": "convex dashboard",
    "dev": "convex dev",
    "format:check": "oxfmt --check \"convex/**/*.ts\" \"!convex/_generated/**\"",
    "lint": "oxlint convex",
    "lint:fix": "oxlint --fix convex && oxfmt --write convex",
    "test": "vitest run --configLoader runner",
    "typecheck": "tsc -p convex/tsconfig.json --noEmit"
  },
  "dependencies": {
    "@convex-dev/migrations": "^0.3.5",
    "@convex-dev/rate-limiter": "0.3.2",
    "@repo/domain": "workspace:*",
    "@repo/stellar": "workspace:*",
    "convex": "^1.42.3"
  },
  "devDependencies": {
    "@edge-runtime/vm": "^5.0.0",
    "convex-test": "^0.0.54",
    "typescript": "5.9.2",
    "vite": "^7.0.0",
    "vitest": "^4.1.10"
  }
}
